/*let return = 10;
//return is a keyword, so it cannot be used as an identifier
//let class = 10;
//class is a keyword, so it cannot be used as an identifier
let myvariable = 10;
//myvariable is a valid identifier
let undefined = 10;
//undefined is a keyword, so it cannot be used as an identifier
let null = 10;
//null is a keyword, so it cannot be used as an identifier

let Infinity = 10;
//Infinity is a keyword, so it cannot be used as an identifier
let notnull = 10;
//notnull is a valid identifier but not a good practice to use it as an identifier
*/